# Browserbase (required for env=BROWSERBASE, the default)
BROWSERBASE_API_KEY=
BROWSERBASE_PROJECT_ID=

# LLM provider key. Either set ANTHROPIC_API_KEY and use "anthropic/..." models,
# OR omit it and use a "gateway/anthropic/..." model to bill the LLM through Browserbase.
ANTHROPIC_API_KEY=

# Optional: override the browser environment. "BROWSERBASE" (default) or "LOCAL".
# LOCAL runs Chromium on your machine and requires: npx playwright install chromium
WAA_ENV=BROWSERBASE

# ── Browser Automation Gateway ──────────────────────────────────────────────
# Port the gateway HTTP server listens on.
PORT=8080

# Postgres for the job store and queue (local dev: docker compose up -d).
DATABASE_URL=postgres://gateway:gateway@localhost:5433/browsergateway

# Local dev ONLY: seeds one admin caller with this bearer token so a fresh
# checkout can talk to itself. Never set in production; issue per-caller
# scoped tokens instead. Without any caller the API is fail-closed.
GATEWAY_DEV_TOKEN=

# Queue concurrency caps.
GATEWAY_GLOBAL_CAP=3
GATEWAY_PLATFORM_CAP=2

# Canaries: interval between scheduled known-record runs per live pair.
# 0 disables the scheduler (admin can still trigger POST /admin/canaries/run).
GATEWAY_CANARY_INTERVAL_MS=0

# Cost accounting: estimated USD per agent step; 0 disables the estimate.
GATEWAY_COST_PER_STEP_USD=0

# Optional: canary failure alerts.
SLACK_WEBHOOK_URL=

# 1Password (JIT credential resolution). The service-account token is the ONLY
# portal secret the container holds; scope it to the portals vault only.
OP_SERVICE_ACCOUNT_TOKEN=
OP_PORTALS_VAULT=Portals

# Local-testing fallback (used only when OP_SERVICE_ACCOUNT_TOKEN is unset).
# One pair per portalKey, e.g. lightreach:
# PORTAL_LIGHTREACH_USERNAME=
# PORTAL_LIGHTREACH_PASSWORD=
# PORTAL_LIGHTREACH_OTP=
